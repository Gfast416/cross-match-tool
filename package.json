{
  "name": "cross-match-tool",
  "version": "1.2.0",
  "description": "Dynamic cross-match scanner for Solana DEX token pairs — live-ready, fee-efficient",
  "type": "module",
  "main": "cross_match.js",
  "scripts": {
    "start": "node cross_match.js",
    "scan": "node cross_match.js",
    "deploy": "node deploy.js",
    "verify": "node verify.js",
    "test": "node --check scanner.js && node --check cross_match.js && node --check deploy.js && node --check verify.js"
  },
  "dependencies": {
    "@meteora-ag/cp-amm-sdk": "^1.4.6",
    "@meteora-ag/dlmm": "^1.9.14",
    "@solana/web3.js": "^1.98.4",
    "axios": "^1.19.0",
    "bn.js": "^5.2.5",
    "dotenv": "^16.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
